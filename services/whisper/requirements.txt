fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12
faster-whisper==1.0.3
requests==2.32.3
# Optional speaker diarization (only needed when ENABLE_DIARIZATION=true).
# These are heavy (torch); leave commented unless you diarize.
# pyannote.audio==3.3.1
# torch==2.4.1
